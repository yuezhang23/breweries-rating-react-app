import { useSelector } from "react-redux";
import { ProjectState } from "../store";

function Home() {
  
  return (
    <div className="container-fluid">
      User Home
    </div>
  );
}

export default Home;
  