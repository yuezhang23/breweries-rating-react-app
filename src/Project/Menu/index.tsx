import { useSelector } from "react-redux";
import { ProjectState } from "../store";

function Menu() {
  
  return (
    <div className="container-fluid">
      Temp Menu
    </div>
  );
}

export default Menu;